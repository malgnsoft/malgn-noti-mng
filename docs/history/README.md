# 작업 이력 (`docs/history/`)

날짜별 작업 내역을 누적해 두는 폴더입니다. **하루에 한 파일** 원칙.

## 파일명 규칙

```
history.yyyyMMdd.md
```

예: `history.20260514.md`. 작업이 있는 날만 생성합니다.

## 파일 구조 (공통)

각 파일은 다음 순서를 따릅니다.

1. **한 줄 요약** — 그 날의 가장 큰 변화
2. **번호별 작업 섹션** — 결정 사항 / 코드 변경 / 배포 / 미세 조정
3. **산출물** — 추가·수정된 파일, 배포 버전, 외부 URL
4. **다음 단계 / 알려진 한계**

## 인덱스

| 날짜 | 요약 |
| --- | --- |
| [2026-06-12](./history.20260612.md) | **`malgn-noti-mng` 관리앱 회원 시스템 풀스택 구축 + 프로덕션 배포** — 직접 회원가입(개인정보 동의·아이디 중복확인·완료 화면)·로그인/세션(HMAC HttpOnly 쿠키)·내 정보(/account 프로필·비밀번호 변경)·참여자 목록(/members)·맑은오피스 연동(서버 간 upsert·SSO 토큰) 임시 구현. 원격 D1(`malgn-noti-project`)에 `member` 테이블(마이그레이션 0002·0003) 직접 적용. Cloudflare Pages 프로덕션 배포 후 회원가입 500 → 원인은 `hashPassword` PBKDF2 반복수 210,000회가 Pages Functions CPU 한도 초과 → `PBKDF2_ITER` **100,000** 하향(verifyPassword는 저장 해시의 iter로 검증해 하위호환)·재배포로 signup/login/me/account/members 전부 **200 GREEN**. 배포 alias `9ceaf05f`, 커밋 `0b4da66` push. 검증 throwaway 계정 정리 완료. **(§4)** 이어 **사이트 전체 로그인 게이트(#2)+문서 통합(#1)** 프로덕션 배포·검증 — 전 페이지 SSR 전환(프리렌더 off) + 전역 라우트 게이트(`01.require-auth.global`)·데이터 API 게이트(`server/middleware/auth`). 비로그인 `/`·`/docs`·`/wbs`·`/history` **302→/login**·`/api/*` **401**, 로그인 후 전 페이지·문서(28행) 렌더 **200** GREEN. 마지막 alias `a40cefb0`. **#3 덤프 누수 차단은 미해결** — 이 앱 `@nuxt/content` 는 D1 백엔드가 아니라 정적 HTTP 덤프(`/__nuxt_content/docs/sql_dump.txt`)를 런타임에 가져오는 구조라, 차단 리다이렉트 시 `/docs`·`/`·`/history` 가 500(콘텐츠 미적재)으로 깨짐을 라이브 확인 → 렌더 보존 위해 차단 철회(덤프 현재 공개 200). 근본 해법=콘텐츠 D1 전환, 후속 권고. **(§5)** **이슈 게시판**(`/issues` 목록·작성·상세·수정, `21bc838`)·목록 테이블 정렬 fix(`table-layout:fixed`+컬럼 폭, `4205411`)·재시드 도구(`1c21522`)의 누락 배포 이력 정합 + 라이브 `/issues` stale 배포(빌드 미반영) **재배포**로 해결. alias `2463c918`, 프로덕션 fix-CSS `200+table-layout:fixed` GREEN. **(§6)** 재배포 후에도 "정렬 안 됨" 리포트 → 프로덕션 SSR HTML 실측(임시 세션)으로 **라이브 DOM·인라인 CSS 가 이미 정상 5컬럼 fixed-layout** 임을 확인, 근본원인은 **HTML 응답 `Cache-Control` 부재로 인한 브라우저 캐시**. `server/middleware/no-cache.ts` 신설(html `no-cache, must-revalidate`, `/_nuxt/*` immutable 유지)·배포 alias `34c31abb`로 재발 차단. **(§7)** **그러나 §5·§6 의 "캐시 탓"은 오진** — Playwright 실브라우저 computed style 실측으로 본문 행 `class="row"` 가 전역 유틸 **`.row{display:flex}`(main.css:1389)** 와 충돌해 `tbody tr=flex`/`td=block` 이 되며 `table-layout:fixed` 컬럼을 무시함을 확정. `app/pages/issues/index.vue` 본문 행 클래스 `row`→`issue-row` 분리로 수정·배포(`b2134bd8`), 헤더·본문 5컬럼 좌표 일치(`ALIGNED:true`) 검증. 교훈: 표 행에 범용 유틸명(`row`/`col`) 금지·레이아웃 버그는 실브라우저로 확정. **(§8)** **이슈 등록/수정 이미지 업로드**(Cloudflare R2 + 본문 마크다운 첨부) — R2 버킷 `malgn-noti-mng-files`(FILES)·`/api/uploads` 업로드/서빙 API(인증 게이트·png/jpg/gif/webp·5MB·UUID·immutable)·`markdown.ts` 이미지 렌더·`AppIssueForm` 첨부 버튼/드래그/붙여넣기. E2E(69B 라운드트립·비로그인 401)+Playwright(`.doc-prose img` 로드)·typecheck·lint GREEN. alias `b80f7bd2`. **(§9)** **이슈 상세 페이지 디자인 정리** — 투박한 회색 액션바 제거·수정/삭제 헤더 우측 이동·상태 select 메타 라인 인라인(상태 라벨 두 줄 깨짐 해소)·제목/여백 정돈. 실브라우저 검증, alias `2fd403a5`. **(§10)** **수정 페이지 라우팅 충돌 수정**(`[id].vue`+`[id]/edit.vue` 공존 시 부모 `<NuxtPage/>` 아웃렛 부재로 `/edit` 가 상세 렌더 → 상세를 `[id]/index.vue` 로 이동·형제 라우트화) **+ 마크다운 인용문 버그 수정**(escape 후 `^&gt;` 매칭). 실브라우저 `EDIT_OK`·`QUOTE_OK` GREEN, alias `d7805887`. **(§11)** 이슈 목록에서 본문 미리보기(`it.preview`) 제거 — 이미지 첨부 시 마크다운 원문이 그대로 노출돼 지저분 → `.row-preview` 삭제(제목만). alias `7d859381`. **(§12)** 이슈 목록 필터 레이아웃 — 상태 select 가 전역 `.select{width:100%}` 충돌로 전체 폭이던 것을 `width:auto` 로 축소·좌상단 배치, 분류 탭·검색(우측 끝) 유지. alias `be6fee5d`. **(§13)** 작업 이력/문서 상세 "문서를 찾을 수 없습니다" — @nuxt/content 가 콘텐츠 D1(`_content_docs`)을 런타임 조회하는데 **docs 변경 배포 시 콘텐츠 D1 재시드를 §1~§12 내내 누락**(+ `gen-content-seed` `;;` 빈 statement 버그로 도구가 한 번도 적용된 적 없음)이 원인. 스크립트 수정 + build→deploy→`content:seed:gen`→`d1 execute` 절차로 원격 `_content_docs` 재시드. `/docs/history/*` 본문 렌더 검증. **운영 규칙: docs 변경 배포는 콘텐츠 D1 재시드까지 필수.** **(§14)** 작성일 **KST 고정**(`formatDate` UTC+9, SSR·클라 동일·하이드레이션 불일치 해소) + **콘텐츠 페이지 클릭 네비 빈 화면 수정** — 목록/상세가 클라 `queryCollection`(콘텐츠 DB 미적재)으로 비던 것을 **서버 API(`/api/docs`·`/api/doc`, D1 조회)+`useFetch`** 로 일원화(SSR·클라 네비 일관). 실브라우저 클릭 네비 20카드·상세 렌더 검증. alias `1d5ad445`. **(§15)** **참여자 권한 등급(관리자/참여자) + 가입 승인 절차 + 이슈 관리자 권한** — `grade` 컬럼 신설(기존 role=직무와 분리)·승인은 `status`(pending→active). 관리자만 참여자 관리(`/api/members` 403 게이트·승인/등급/삭제 API)·GNB 참여자 메뉴·전체 이슈 수정/삭제. 가입은 세션 미발급→관리자 승인 후 로그인(login 403 pending). 기존 dotype 관리자 부트스트랩(마이그레이션 0004). API·실브라우저 권한 전 흐름 GREEN. alias `f1d9fc68`. **(§16)** **이슈 답글(댓글) 기능** — `issue_comment` 테이블(마이그레이션 0005) + 목록/작성/삭제 API(`/api/issues/[id]/comments`, 작성자·관리자만 삭제) + 상세 페이지 답글 섹션(작성 폼·KST 날짜·삭제). API·실브라우저 검증 GREEN. alias `315dc34d`. **(§17)** **답글 마크다운 렌더 + 이미지 업로드** — 이슈 본문 에디터를 공용 `AppMarkdownEditor`(MD 입력·이미지 첨부/드래그/붙여넣기)로 추출해 이슈 본문·답글 동일 동작. 답글 표시는 `renderMarkdown`→`.doc-prose`. 실브라우저 굵게·이미지 렌더 검증. alias `9d87f853`. |
| [2026-06-11](./history.20260611.md) | **3차 멀티에이전트팀(`account-pages-dev`, 6역 tmux 분할·모델 혼합)로 계정 6페이지 풀스택** — cards·security·multi·credit·inquiries·billing을 **데모→실 API 연동**. 백엔드 무스키마 3엔드포인트 신설(`GET/PUT /me/security` 2FA토글·`/me/members` CRUD 즉시생성·`GET /credit-ledger/:id/receipt`), `AppBillingPanel` 신규, `inquiries/[id].vue` 동적 라우트(구 detail.vue 제거), 기획 정본 6종(`docs/pages/`). QA 회귀 게이트 GREEN(noti tc PASS·lint 14≤15·api tsc PASS) + 6페이지 엔드포인트 코드 일치. 2레포 커밋(`6e27329`·`e076622`) push + 배포(api Workers `2b7f9fcf`·frontend Pages `87da3ace`). 모델: planner/api/frontend=opus, designer/publisher=sonnet, qa=haiku. 모든 선택 추천/1번 자체 결정. **(§7 후속)** admin-dev(7역) 합류 — 크레딧 충전(`/charge`, `POST /me/charge` mock·Idempotency-Key)·관리자 발신번호 심사(`/senders/numbers`, `/ops/sender-phones`)·**문의 작성 저장 버그픽스**(`onSubmit`이 POST 미호출이라 등록 미저장→수정). 3레포 커밋(`41b1fa5`·`faf40c4`·`c8b9d64`) push + 배포(api `34da178d`·admin `fdf96728`·frontend `71b85e64`). "나의 페이지" 전 메뉴 ✅ 실연동 검수. **(§8)** 보안 재인증 422 버그픽스 + **멤버 최초로그인 온보딩**(joinState invited→joined·약관·비번변경) + **막혔던 DB 마이그레이션 5종(`0007`~`0011`) Aurora 적용**(`db.malgn.co.kr` 직접 mysql 직결로 진단·해결, `VARCHAR(16383)×utf8mb4` 행크기→MEDIUMTEXT) + 관리자 **Wave1**(운영자 1:1문의·accounts PII 마스킹/보기 reveal+감사로그) + **Wave2**(운영자 계정·권한그룹+권한매트릭스·FAQ·공지, Figma 12_시스템/9_고객지원, 신규 테이블 TB_OPERATOR/TB_OPERATOR_ROLE/TB_FAQ/TB_NOTICE). 배포: api `081a27dd`·admin `ee2a120c`→`adaf06cc`·noti `e97a1c4f`. QA Wave1·2 GREEN. **(§9)** 발신정보 검수 확장(이메일 도메인·카카오 프로필 심사, 실 스키마 enum 차이 반영) + **사용자단 발신번호 관리 목업→실연동 버그픽스**(등록 미저장 문제) + **관리자 Figma 전수 감사**·Phase A·B 보강(operators PII 마스킹/보기·일괄·CSV / roles 도메인×액션 매트릭스·인원모달 / faq·notices·inquiries 필드, 마이그레이션 `0012`~`0015` direct mysql 적용). 배포 api `41f49658`/`c823730f`·admin `e098e23e`/`2da97612`·noti `da097b4d`. QA 전부 GREEN. Phase C 보류(운영자 로그인·서류 AI검증). **(§10)** 로그인 이력 마스킹/보기·검색 보강(`/ops/login-history` reveal 페이지 토글·감사로그) + **고객사(customers) Figma 정본(`figma_noti_user` 14프레임) 풀빌드** — Phase1(기본정보 편집·크레딧·발신정보 실연동) + **Phase2 순차**(2a 담당자 대표계정 편집/마스킹·2b 계약관리 stepper·서류 PDF다운로드·2c 발송통계 사전집계 차트[실발송=발송−발송실패]·2d 결제 선불/환불·영수증 모달[후불·PG액션 비활성]·2e 등록 풀페이지 3탭). 전부 무스키마(기존 테이블·`TB_DISPATCH_STAT_DAILY` 재사용). 배포 api `e6257857`→`9c920670`·admin `bcacbe07`→`19d2ed11`(7회). 단계별 QA GREEN. 별도 에픽 분리(단가 TB_PRICING·담보·후불 invoice·통계 hour·우편번호·서명요청). **(§11 admin-dev)** 관리자단 LNB — AI 배너 닫기/하루 비노출 + **전체 메뉴 사양(13그룹/55항목)** 교체(수신거부·콘텐츠·API 독립 그룹 신설) + 신규 42라우트 '준비 중' 스텁(`AppComingSoon`) + LNB 중복 활성화 버그픽스(최장 경로 매칭) + 고객사 필터 일렬형(`AppFilterBar` 옵트인 `inline`). `malgn-noti-admin` 5커밋·5배포(`69c788bd`·`1b6e0584`·`9b2de4ec`·`a3facf3a`·`79355d31`). |
| [2026-06-10](./history.20260610.md) | **WBS 스크롤 개편(solsol 방식) + admin 고객사 상세 발신정보 섹션** — `malgn-noti-mng` ① `/wbs` 전체화면 스크롤을 이중 스크롤(sticky pane) → **간트 단일 스크롤 + GNB·상단 부드럽게 접힘**(공유 상태 `wbsChromeHidden`, solsol 이식)으로 교체 ② 스텝 행에 **비중 표시**(현황판과 동일 `비중 N% · 진행 N%`) + **기준일 클릭 시 오늘 컬럼으로 가로 스크롤** ③ 블루프린트(`PROJECT_MANAGEMENT_BLUEPRINT.md`) 현행화(WBS CRUD·풀스크린 스크롤·실시간 기준일·날짜 ISO/FK/코드줄바꿈 함정 4종) + `project-mng-starter.zip` 재패키징 ④ `/docs/board` 빈 본문 버그 수정(`useAsyncData` 키 함수→문자열). `malgn-noti-admin` ⑤ 핸드오프(`ProjectHandoff/figma_noti` 18장=운영자 콘솔) **회원/고객사부터 Figma 매칭 시작** — 고객사 상세에 **발신정보 6테이블**(발신번호·브랜드RCS·도메인·PUSH·알림톡 프로필·080) 추가(`Deatail.png`/`정보.png` 범례 반영) + **고객사 그룹(주소록) 페이지** 개발(목록·그룹 수정 모달·그룹 연락처 모달) + **고객사 상세 전면 재구성**(KPI 헤더 + 7탭 기본정보/담당자/계약관리/발신정보/발송통계/크레딧/결제 + 우측 메모 패널) + **단가 모달**(`AppPricingDialog` 공용, 목록·상세) + **로그인 이력 / 탈퇴 계정** 페이지 개발 + Pages 배포(alias `4520c8ab`·`e11d362f`·`3f61d7e5`). 데모 데이터 기반, 발신정보·통계·크레딧·결제·그룹은 백엔드 연동 대기. ⑥ 화면 보강·위험액션 사유·스크롤락 누적 변경 검토·배포(`03aa7735`). **⑦ 멀티에이전트 개발팀(8역) 구성** — WBS 김도형 이번 주(6/8~14) 작업 14태스크 일괄: `malgn-noti-api` 크레딧 차감 결선·발송 취소/환불·고객사 `statusReason` 감사로그(`TB_AUDIT_LOG`)·비밀번호 재설정 API + `schema.ts` 라이브 정합(캠페인 5테이블·파티션 인덱스·감사로그) + 사용자단 인증 3페이지(reset-password·security) + 화면설계서 9종(SEND/HISTORY/CONTACTS/SENDER/CAMPAIGN/MANAGE + admin 3종) + QA typecheck **5게이트 GREEN**. **4레포 14커밋 origin/main push**(6/4 인간 WIP=이메일변경 기능은 경로 분리 제외). **⑪ 2차 멀티에이전트팀(`auth-member-dev`, tmux 분할 6역)** — 회원·인증 도메인 풀스택: 사용자단(로그인 2FA·비번 변경·회원 탈퇴), 관리자단(회원관리 4페이지 실 API + Nitro 프록시 6종), 백엔드(2FA·`/me/password`·`/me/withdraw`·관리자 accounts API·감사로그). QA 정적 cross-check **계약 6/6 GREEN**·회귀 0(라이브 스모크는 Aurora 필요로 미수행). 3개 코드 레포 분리 커밋(`d613c76`·`0cd3149`·`a799e4a`) origin/main push. 계약 thrash(2FA 네이밍 2.5회)는 리터럴 고정+한쪽 동결+QA 코드 대조로 수습. |
| [2026-06-09](./history.20260609.md) | **SMS 실발송·iPIN 검증 + 관리자단 고객사 페이지 풀스택 실연동(배포)** — ① `malgn-noti-api` **문자(SMS) 실작동 확인**(010-3283-5673 NHN 경유 실발송 성공, 실 messageId·code 0), **iPIN(NICE)은 real 불가**(`1007 허용되지 않은 IP 접근` — Cloudflare egress IP 미등록 → mock만). ② **고객사 `/customers` 목록·상세 정적 목업 → 풀스택**: api 운영자용 `/ops/companies`(교차-테넌트, `X-Ops-Token`=`OPS_TOKEN` 게이트, Aurora/Drizzle) 신설 + admin Nitro 서버 프록시(토큰 서버측 주입) + 실데이터화(필터·검색·서버 페이징·승인/반려/정지 PATCH·CSV). 워커 + Pages(alias `4ac6f26e`) 프로덕션 배포 + 실데이터 전 구간 검증. ③ (같은 날) 동그라미 이니셜 `(주)`/`주식회사` 접두어 제거 + **고객사 등록**(`POST /ops/companies` — 회사+소유자 계정 생성, loginid 중복 409) 추가·배포(alias `1667b405`). |
| [2026-06-05](./history.20260605.md) | **malgn-noti-mng 관리 레포 신설** — GitHub 연결 + `malgn-noti/doc/` 트리 집약(공통·도메인·이력) + CLAUDE.md 병합·현행화(이력 작성처를 본 레포로 변경, malgn-noti `doc/history/` 삭제) + malgn-noti 동일 스택(Nuxt 3 + Tailwind v4 + Nuxt UI v3) + `@nuxt/content` 기반 **문서/이력 브라우저 앱** 스캐폴딩(대시보드·문서·이력 타임라인) + **Cloudflare Pages 정적 배포**(<https://malgn-noti-mng.pages.dev>, 프리렌더 라우트 직접 열거로 마크다운 내부 링크 크롤·대소문자·파일명 점 이슈 해결) + **맑은노티 현황판(`/board`)** — malgn-noti `/wbs` 읽기 전용 이식(공개 API `GET /wbs` 라이브) + 대시보드 WBS 현황 요약(`AppWbsOverview` 공유) |
| [2026-05-11](./history.20260511.md) | 프로젝트 착수 — 세 레포 부트스트랩 + 첫 GitHub 푸시 + Cloudflare 첫 배포 + 시안 GNB 도입 |
| [2026-05-12](./history.20260512.md) | 시안 정밀 매칭 — 콘텐츠 폭 1200px 통일, Utility Bar 분리, 메뉴 8개, 문서 3종(`FRONTEND`/`STACK`/`DESIGN`) |
| [2026-05-13](./history.20260513.md) | Smart Placement + 발송 페이지 풍부화 — 공용 컴포넌트 21종, `useChannelMeta`, SMS/알림톡 발송 페이지 |
| [2026-05-14](./history.20260514.md) | GitHub 일괄 푸시 + `doc/history/` 폴더 정리 |
| [2026-05-18](./history.20260518.md) | 디자인 시스템 전면 피벗 — Relay-inspired(핸드오프 정본), 셸+홈+발송 전 채널 적용, `design-system-pivot` 푸시 |
| [2026-05-19](./history.20260519.md) | 디자인 가이드 페이지(`/guide`, 18섹션 라이브 카탈로그) + Cloudflare Pages 프로덕션 배포 |
| [2026-05-20](./history.20260520.md) | 발송 페이지 UX 폴리시 2차 + PUSH 부가항목·플로우 관리 + 문서 현행화 + 발송 조회 페이지 재작업·btn-sky 제거 + 통계 페이지 재구성(Chart.js)·폰트 토큰화·zoom 제거 + 발송 템플릿 토글 개선 + 주소록 관리 페이지 강화 + 발신 번호 관리 페이지·등록 마법사 + 그룹 관리 페이지 + RCS 브랜드 관리 페이지 + 이메일 도메인 관리 페이지 (배포 #15~#26) |
| [2026-05-21](./history.20260521.md) | 발신 정보 페이지 마무리(발신 프로필·PUSH 인증·080 수신 거부) + 테이블 스타일 A/B/C 정의·발송 조회 툴바 재배치 + GNB 정리 + 수신 거부 관리 3종 + 문자메시지·알림톡 템플릿 관리 페이지 + 미리보기 시각 현재화 + 로그인 페이지 시안 IA 재구성 + 비밀번호 재설정 입력란 보정 + 메시지 관리 5채널 템플릿 페이지(SMS·알림톡·RCS·이메일·PUSH) + 보안 인증 페이지 보정 + 사이트맵 페이지 + 회원가입 5단계 마법사 + 메시지 상세 설정 페이지 + 새 비밀번호 설정 페이지 보정 + 랜딩페이지 만들기 + 문의하기 페이지 /account/inquiry 이동 + 나의 페이지 섹션 + 크레딧 충전 플로우 + API 루트 → /doc 리다이렉트 (배포 #27~#46 + API) |
| [2026-05-22](./history.20260522.md) | 나의 페이지 화면 신규 구성 — 비밀번호 변경·보안로그인 설정·멀티 계정 추가·계약 관리(계약서 확인·3스텝 전자서명 위저드·PDF 미리보기) + 크레딧 관리(A형 내역 테이블·영수증 모달) + 나의 문의(목록·상세 댓글 스레드)·문의하기 LNB + 서비스 담당자 초대 플로우(/invite 등록 페이지)·문의 등록 완료 페이지 + 사이트맵 현행화 + 운영 가이드 페이지(/help)·GNB 연결 + 비로그인 공개 랜딩 페이지(/, 히어로·장점·채널 단가 비교) (배포 #47~#52) |
| [2026-05-26](./history.20260526.md) | `malgn-noti-api` 데이터 모델·초기 DDL·Hyperdrive 연결·첫 프로덕션 배포 — 49 테이블 데이터 모델(TB_·company_id·status INT 1/0/-1 + *_state·*_yn·loginid/email 분리) + Mermaid ERD 9종 + 확장성 전략(월 RANGE 파티셔닝·Hot/Warm/Cold·R2 오프로드) + 0000_initial.sql(49 테이블, 파티션 5종) + Hyperdrive(`a2ba4efe...`) 바인딩 + drizzle-orm/mysql2 + /health/db + `wrangler dev --remote`로 로컬도 실제 Aurora + `https://malgn-noti-api.malgnsoft.workers.dev` 프로덕션 첫 배포(/health/db → mysql_version 8.0.42) + `malgn-noti-api/CLAUDE.md §8.1`에 배포·Git·작업 이력 운영 컨벤션 명문화 + malgn-noti 프론트 배포 #53 (list 툴바 새로고침 버튼 일괄 제거 + guide/DESIGN 갱신) + `/admin/migrate`·`/admin/tables`·`/admin/partitions` 라우트 + 0000_initial.sql Aurora 적용 (49 테이블 + 75 파티션 라이브) + 기본 CRUD API 골격 (errors/pagination/auth/Drizzle schema + /me /contacts /contact-groups /sender-phones) + `/doc` API 가이드(Scalar UI + OpenAPI 3.1) + malgn-noti-api 프로덕션 배포 #2 (Version `1fdc3b12...`, 가드 작동 확인) + 14개 도메인 라우트 추가(발신정보 5종/optout-entries/templates 2종/inquiries/company-settings/payment-methods/landing-pages/credit-ledger) + Phase 1·2·3 (/doc 확장 paths 10→37·schemas 11→45 + signup/login + PBKDF2 + JWT HS256 + 발송 이력 read-only with 90일 윈도우 강제) + malgn-noti-api 프로덕션 배포 #3 (Version `926017d2...`, JWT_SECRET secret, 실제 /auth/signup → /me 동작 확인) + POST /send/sms 발송 producer(DB 적재까지 — 발신번호 검증·옵트아웃 필터·크레딧 hold·트랜잭션, 알려진 멱등 버그 TODO) + malgn-noti-api 프로덕션 배포 #4·#5 (Version `4d9e1fbe...` → `afaa4c89...`, /send/sms 라이브 가드·검증 흐름 9건 + 재배포 검증 4건, jwt.ts linter 캐스트 명시화) + 🐛 멱등 버그 해결 (TB_IDEMPOTENCY + INSERT-then-conflict race-free 패턴, 검증 3건 통과) + NHN SMS 어댑터(mock/real) + Cloudflare Queues(`malgn-noti-dispatch`) + consumer worker (dispatch_state 천이) + 프로덕션 배포 #6 (Version `b30dc2a3...`, Producer+Consumer 동시 바인딩 라이브, 큐 e2e는 Cloudflare 1105 회복 후 재검증) |
| [2026-06-04](./history.20260604.md) | **§1** NICE IPv6 진단 — 6/2 §16의 1007 차단 후속. 사용자 옵션 B(NICE 콘솔에 Cloudflare egress IP 대역 등록) 진행 후 재시도 → 여전히 1007. 진단용 `/diag/egress` 임시 endpoint로 `api.ipify.org` 8회 호출 → Workers outbound가 **IPv6 `2a06:98c0:3600::103`** (Cloudflare 공식 IPv6 `2a06:98c0::/29` 소속)임을 확인. NICE에 IPv4만 등록되어 IPv6 출발지 거부가 원인. 사용자에게 Cloudflare IPv6 대역 7개 추가 등록 또는 IP 검사 OFF 안내 후 보류. 진단 endpoint 즉시 제거 + Workers 재배포. CLIENT_ID/SECRET 덮어쓰기(값 일치 보장만). **§2** Hyperdrive 교체 — `a2ba4efe...` → `439b109d...`. 신규 origin host `malgn-dev-db.apiserver.kr` (Cloudflare Tunnel 엔드포인트) + `access_client_id`. Aurora 노출이 "퍼블릭+SG egress IP 화이트리스트"에서 "Tunnel(Access) 기반"으로 전환 — egress IP 동기화 부담 해소, CLAUDE.md §12 TODO "SG 갱신 운영 절차" 항목 자연 달성. 코드 변경 0. 라이브 검증(GET /health/db mysql_version 8.0.42 + POST /auth/nice/init DB 쓰기 정상). 정본 3개 동기화 — API CLAUDE.md §3·§8·§12, SCALABILITY.md §6 신규 절(전/후 비교 + 전환 이유 + 운영 영향), MIGRATION.md §1 통로 다이어그램에 Tunnel 단계 추가. Workers 배포 Version `a457b7dc...`. **§3** 관리자단 핸드오프 풀세트 — `handoff_noti_admin` (3,129줄 jsx)를 Vue 3 + Nuxt UI v3로 1:1 포팅. 셸 완전 재정비(AppLnb 핸드오프 정본 9 그룹/17 라우트로 교체 + AppTopbar useState 기반 동적 브레드크럼 + useBreadcrumb composable) + 공유 컴포넌트 14종(PageHeader/SectionCard/Tabs/Segmented/FilterBar/DateRange/DataTable generic+`#cell-{key}` scoped slot/Pagination/StatusBadge 값→톤 자동 매핑/ChannelChip/StatCard accent 7종/Drawer/Modal Teleport+scroll lock/Field/EmptyState) + 차트 4종(BarChart 그룹 막대/AreaChart SVG path+그라데이션/Donut stroke-dasharray/ProgressBar) + **17 페이지**(대시보드·고객사·고객사 상세·계정·모니터링·발신번호·발신프로필·템플릿검수·결제·채널단가·충전쿠폰·1:1문의·FAQ·공지·통계·운영자·권한그룹·API). app.config.ts `info: 'indigo'` 매핑으로 핸드오프 indigo 강조색을 Nuxt UI semantic으로. 18 라우트 라이브 200. **§4** 폰트 사이즈 정합화 — 사용자 보고로 두 원인 동시 발견: (a) main.css `html,body{font-size:13px}`가 Tailwind 토큰(16px base 가정)을 모두 18% 축소시킴 — 핸드오프는 base font-size 명시 없이 16px Tailwind 기본 사용 + `letter-spacing: -0.01em`. (b) 직전 turn의 cwd가 `/malgn-noti`(사용자단)였고 거기서 빌드한 dist를 `wrangler pages deploy --project-name=malgn-noti-admin`으로 **사용자단을 admin 프로젝트에 잘못 배포**한 상태였음(chunk 600개 / AppGnb·AppContractPanel·AppCardAddDialog 등 사용자단 chunk 혼입). 둘 다 정정 — main.css 13px 제거 + letter-spacing -0.01em 추가, admin 디렉토리에서 clean rebuild + 재배포 alias `8852d5da.malgn-noti-admin.pages.dev` (chunk 96개로 정상화). 교훈으로 멀티 레포 deploy 가드(prebuild name 체크 + chunk 수 sanity)·base font-size 명시 금지 패턴 정리. malgn-noti-admin 커밋 `0227cae` + `1b63200`. **§5** WBS 페이지 편집 — DB 미사용/R2 단일 JSON 객체 정본(`wbs/wbs.json`, FILES 바인딩). API에 GET 공개 + PATCH 인증 2 라우트 + 142 task 시드(`src/data/wbs-seed.ts` + `src/routes/wbs.ts`). 사용자단은 임베디드 STAGES 제거 → top-level `await api('/wbs')` + AppModal 인라인 편집 다이얼로그(owner·note·href·targetDate·completionDate 5 필드, 빈값=`null`=필드 제거). 비로그인은 읽기 전용 + "로그인하면 편집 가능" 힌트. 동시 편집은 last-write-wins. malgn-noti-api 커밋 `9945db3` Workers Version `28f3e6a8...`, malgn-noti 커밋 `3ed473e` Pages alias `02bb58e6`. 후속(§5.9) 목표일·완료일 `YYYY.MM.DD` 포맷 통일 + `<input type="date">` 캘린더 위젯 + API Zod regex `^\d{4}\.\d{2}\.\d{2}$` 강제. 레거시 `5/8` 값은 표시 시 2026 기준 `2026.05.08`로 자동 변환, 다음 PATCH 시 R2에도 정합화. malgn-noti-api `3a35464` Workers Version `eb02206c...`, malgn-noti `08e5c33` Pages alias `98bd09e2`. **§6** NHN Notification Hub OAuth 어댑터 + Email 실 발송 + 서비스 담당자 이메일 변경 라우트 — Notification Hub 신규 통합 서비스의 인증이 OAuth2 client_credentials → Bearer 토큰임을 확인 후 어댑터 전면 재작성. `src/adapters/nhn/oauth.ts` 신규(`https://oauth.api.nhncloudservice.com/oauth2/token/create` Basic Auth + `scope=appKey:{APP_KEY}` 토큰 발급 + 메모리 캐시). SMS·Email 어댑터 `POST {base}/message/v1.0/{SMS|EMAIL}/free-form-messages/{purpose}` + `contactType=PHONE_NUMBER`/`EMAIL_ADDRESS` + `X-NC-APP-KEY` + `X-NHN-Authorization`. `NhnCredentials` 확장(userAccessKey/secretAccessKey + legacy secretKey 옵셔널). Email은 `message@malgnsoft.com` 발신 도메인 NHN 콘솔 등록 + `EMAIL_FROM`/`EMAIL_FROM_NAME` secret 등록 후 실 발송 검증 통과(`messageId=20260604154608lR6MBAKn8v0`). 동시에 `POST /me/email-change` 신설 — 비밀번호 + OTP(`purpose=change_email`) 검증 후 **`user.email` 만** UPDATE(`loginid` 가입 시 식별자로 고정 유지). 사용자단 `AppEmailChangeDialog` + auth store `changeEmail()` 실 API 연동. **§7** WBS 현행화 — R2 정본(`wbs/wbs.json`) + `doc/WBS.md` 동시 갱신. Step 5 진척 48% → 55%, 가중평균 약 47.5%. 9 신규 task(5-2-19/20/21 WBS R2 / 이메일 변경 / NHN OAuth 어댑터, 5-3-15 `/wbs` 인라인 편집, 5-3C-20 이메일 변경 다이얼로그, 5-4-14/15/16 핸드오프 17 페이지 + dev 라벨 + 관리자 로고, 5-5-10/11/12 Hyperdrive Tunnel / NHN Email real / NHN SMS pending) + 5-3C-7 완료 승급 + 5-2-16·5-5-5 in_progress 승급. 추가로 사용자 요청으로 김도형 담당 done은 t=c 통일, pending·in_progress 36건은 7/3 마감으로 W1~W5 일정 분배(R2 + doc/WBS.md 동시 적용). **§8** 서비스 담당자 이메일 변경 — 세 가지 버그 동시 fix. (a) `/auth/email-code/verify` 가 `purpose='change_email'` 에서 `consumed_at` 마킹 → 직후 `/me/email-change` 의 OTP 재검증 실패하던 이중 소비 버그를 verify-only 분기로 수정. (b) 비밀번호·OTP 오류 401 → `useApi.onResponseError` 자동 로그아웃 트리거 → 새 헬퍼 `errors.unprocessable()`(422) 도입, 본인 재인증 실패 코드 분리. (c) 다이얼로그 `submit()` 데드락(부모 close 대기) → `onConfirm: (p) => Promise<void>` 콜백 prop 패턴 전환, 다이얼로그가 자기 상태 직접 관리(성공·실패 모두 잠금 해제). malgn-noti-api Workers Version `772adb3b...`, malgn-noti Pages alias `cd6a5bb3`. **§9** 광고성 메일 수신 동의/거부 일시 기록 — DDL 0006 (`TB_COMPANY.ad_receive_at DATETIME NULL`) Aurora 직결 적용 + `PATCH /me/company` 에서 `adReceive` 변경 시 `adReceiveAt: new Date()` 동시 set + `readContext` 4 응답 라우트(GET /me · PATCH /me · PATCH /me/company · POST /me/email-change) 모두 포함. 사용자단 패널에 `[🕒] YYYY.MM.DD HH:mm 광고성 메일 수신에 동의/거부함` 회색 칩 노출 + 토스트 description 처리 일시 표기. 정보통신망법 50조 동의 증빙용. malgn-noti-api Workers Version `3671ce95...`, malgn-noti Pages alias `0f43b158`. |
| [2026-06-02](./history.20260602.md) | 회원·인증 트랙 첫 날. **§1** WBS 구조 개편 — 사용자단을 **5-3A 화면 UI 구성**(목업) + **5-3M 매트릭스**(도메인별 UI/API/연동 한눈에) + **5-3C 화면 ↔ API 연동**(실 데이터) 3 트랙으로 분리. 5-3-15 단일 항목을 16개 도메인별 5-3C-1~16으로 펼침. Step 5 진척률 55%→40%, 전체 가중평균 45%→38%. Pages 배포 #51(alias `bca573ce`). **§2** 로그인 UX 개선 — `POST /auth/login-by-email` 신설(이메일+비번 → 회사 자동 찾기, 단일 매치 즉시 토큰 / 복수 매치 시 `multipleCompanies:true + companies[]`). 프런트 `login/index.vue`에서 **고객사 ID 필드 완전 제거**, 복수 매치 시 회사 선택 카드 UI. Workers 배포 #10(Version `a6197cc7...`), Pages 배포 #52(alias `292da05d`). 라이브 e2e 5 시나리오 통과. WBS 5-3C-4 ⚪→✅. **§3** `TB_USER.loginid` 전역 UNIQUE 정합화 — 정책상 한 이메일이 여러 회사에 가입되어선 안 됨. `0003_user_loginid_global_unique.sql` 신규 + 라이브 적용(`uq_user_company_loginid` 복합 DROP → `uq_user_loginid` 단독 ADD). schema.ts에 `.unique('uq_user_loginid')` 명시. login-by-email 단순화(`for-of` 다중 verify 루프 제거, `.limit(1)`) + OpenAPI에서 `MultipleCompaniesResponse` 삭제 + 프런트 회사 선택 UI(~80줄) 제거. 사전 검증용 임시 데이터 cleanup(회사 8 + 사용자 12). Workers 배포 #11(Version `f7f42855...`), Pages 배포 #53(alias `f150ea0a`). 라이브 e2e 4건 통과(signup 정상 / loginid 중복 409 / login-by-email 단일 토큰 / UNIQUE 인덱스 단독 확인). "한 이메일 = 한 회사 = 한 로그인". **§4** 휴대폰 SMS OTP 라우트(`/auth/phone-code/send`·`/verify` 이메일 OTP와 동일 패턴, `target_type='phone'`) + signup.vue Step 4 실 API 연동 + useApi.ts 401 처리 분리(`/auth/*` 호출은 호출자 처리 — 가입 도중 OTP 잘못 입력 시 페이지 이동 버그 수정) + 가입 완료 화면 고객사 ID 노출 제거 + 토스트 위치(오른쪽 위) + 크기 강화(17px·440px). Workers #12(`84056c86...`), Pages #54~#58. **§5** NICE 통합인증(휴대폰 본인확인) 인프라 — doc/NICE_AUTH.md 신규 정본(12 섹션) + `0004_user_nice_auth.sql` 라이브 적용(TB_NICE_AUTH 신설 + TB_USER에 birthdate/gender/national_info/ci/mobile_co + UNIQUE ci) + NICE 어댑터(mock/real, AES-256-GCM + PBKDF2 + HMAC, Workers Web Crypto만) + 3 라우트(init/callback/status) + /auth/signup 확장(niceSession 검증·CI 중복 차단·NICE 결과로 이름·휴대폰·생년월일 덮어쓰기·state='consumed' 재사용 차단) + signup.vue Step 4 통째로 NICE 흐름으로 교체("본인 인증하기" 버튼 + popup + 폴링 + 결과 표시) + NICE_MOCK secret 적용(자격증명 발급 전 mock 통과). Workers #13(`2ab47c1f...`), Pages #60(`c9577894`). 라이브 e2e 6 시나리오 통과. **§6** `/account/settings` 실 API 연동 — 백엔드 `GET /me` 응답을 TB_USER 13 + TB_COMPANY 14 컬럼 풀로 확장 + `PATCH /me`(name·phone) + `PATCH /me/company`(companyPhone·billingEmail·adReceive, owner/admin) 신설. 사용자단 `AppMemberInfoPanel.vue` 전면 교체 — 목업 데이터 제거 + useAuthStore 기반 실 데이터 + 광고수신 토글 즉시 PATCH + 저장하기는 변경 필드만 한 번에 PATCH. Workers #14(`22368d14...`), Pages #61(`ea35651d`). 라이브 e2e 5건 통과. WBS 5-3C-7 ⚪→🟢. **§7** 사업자등록증 심사 승인 게이트 — 새 정책: 법인/개인사업자 가입 후 승인 받아야 서비스·정보 수정 가능, 개인은 즉시. 0005 라이브 적용(TB_COMPANY에 company_type/approval_state/rejected_reason + 기존 5행 'approved' 호환). signup 자동 분기 + PATCH /me·/me/company에 승인 게이트 403 + /me 응답에 승인 정보 노출. 사용자단 — signup.vue Step 5 분기 + AppMemberInfoPanel에 승인 배너(pending=warning, rejected=danger+사유) + 모든 입력 disabled. Workers #15(`6e47d50b...`), Pages #64(`56e94e5b`). 라이브 e2e 8 시나리오 통과(법인 pending / 수정 403 / 개인 통과 / 운영자 승인 후 통과 / 반려 사유 노출). WBS 5-3C-6 ⚪→🟢 + 5-3C-17 ✅. **§8** 승인 게이트 전 도메인 일관 적용 — `requireApproved()` 미들웨어를 `src/middleware/approval.ts`로 추출 + 18 도메인 라우트에 일괄 적용(`mutate-only` 옵션 — GET 통과, POST/PATCH/PUT/DELETE만 차단). `/inquiries`는 예외(승인 관련 문의 가능). 자동화 스크립트(grep + perl)로 18 라우트의 import + use 라인 일관 갱신. Workers #16(`798bf6f5...`). 라이브 e2e 6 시나리오 통과(GET 통과 / POST 4건 403 / 개인 통과 / 문의 작성 차단 안 됨). **§9** 사용자단 승인 게이트 UI 일관화 — `AppApprovalBanner` 신규(layout 최상단·GNB 위 글로벌 띠, pending=warning·rejected=danger+사유) + `middleware/approval.global.ts` 신규(차단 페이지 접근 시 `/account/settings`로 리다이렉트, 허용은 `/account`·`/home`·`/help`·`/guide`·`/wbs`·`/inquiry`) + `/home` 미승인 분기(KPI/채널 대신 `approval-hero` 큰 안내 카드 + CTA). Pages #65(`2eec9e0b`). **§10** 미승인 진입점을 `/account/contract`(계약 관리)로 변경 — 정책상 사업자등록증 제출/재제출 화면이 미승인 흐름의 메인. middleware ALLOWED_PREFIXES에서 `/home` 제거 + 리다이렉트 대상을 `/account/settings` → `/account/contract`. AppApprovalBanner CTA "계약 관리". signup.vue Step 5 사업자/개인 분기("계약 관리로 이동" / "대시보드로 이동"). /home.vue §9의 미승인 분기 코드 제거(미들웨어가 차단해 진입 불가). Pages #66(`5256d66d`). **§11** `/account/contract` 실 API + R2 첨부 인프라 — R2 bucket `malgn-noti-files` 생성 + `wrangler.toml` FILES 바인딩 + `schema.ts`에 라이브 DDL 매칭(`TB_CONTRACT`·`TB_CONTRACT_FILE`) + signup corp/sole 분기에서 'initial' 이용계약 자동 생성. `/contracts` 라우트 신규 5종(list · :id/sign · files list · files upload(multipart) · files download(stream) · files delete) — PDF·10MB 제한, name 접두사(`사업자등록증_…`·`대부업등록증_…`·`지급이행보증보험증권_…`)로 종류 구분(`TB_CONTRACT_FILE`에 kind 컬럼 없음), 회사 단위 권한(companyId 매칭), renew 체결 시 다른 done 자동 expired. OpenAPI 5 paths + 2 schemas. 사용자단 `AppContractPanel.vue` 전면 교체 — 목업 제거 + `await Promise.all` SSR + FormData 멀티파트 업로드 + 미리보기는 인증 fetch → blob → object URL(iframe Auth 헤더 우회). Workers #17(Version `7213946f...`), Pages alias `9808fe42`. 라이브 e2e 4 시나리오 통과(corp signup auto-contract / 파일 업로드 R2 / 파일 목록 / 체결 → done+expires +2y). **§12** 사업자등록증 첨부 시 회사 승인 상태 'reviewing' 자동 전이 — `approval_state` enum 4단계 확장(`pending → reviewing → approved | rejected`, DDL 변경 없음 — `VARCHAR(20)`). 백엔드 `POST /contracts/files` kind=biz 업로드 후 회사 상태가 `pending`/`rejected`이면 `reviewing`으로 UPDATE(rejected_reason은 그대로). `requireApproved` + `me.ts` PATCH 차단 메시지에 reviewing 분기 추가. 사용자단 `AuthCompany.approvalState` 타입 확장 + `AppApprovalBanner`·`AppMemberInfoPanel` 3분기 안내문 + `AppContractPanel` 패널 상단 상태 카드(pending=warning · reviewing=info · rejected=danger) 신규. `pickFile` 성공 후 `auth.fetchMe()` 호출 → 글로벌 띠·페이지 배너 즉시 "심사 중" 전환(새로고침 불필요). Workers #19(Version `f6877b91...`), Pages alias `d9a82bfa`. 라이브 e2e 통과(corp 가입 직후 pending → biz 업로드 → reviewing + PATCH /me 403 메시지 변경). **§13** /account/contract 첫 진입 회복 — §11/§12 배포 이전 가입자(회사 16) 두 race 보고에 따라 `GET /contracts` lazy auto-create(corp/sole + 계약 0건이면 'initial' 자동 INSERT) + `GET /contracts/files` 자동 회복(회사 pending + biz 파일 1건 이상이면 reviewing UPDATE) 추가. 회사 16은 즉시 DB UPDATE로 backfill. 프런트 `AppContractPanel` SSR fetch 실패 swallow + onMounted 재시도. Workers 패치 `35e2ec85` → `456b73c2`. **§14** 사업자등록증 파일 행 상태 배지 + 반려 시 삭제(Pages `7675ce8f`) — 회사 approval_state를 모든 biz 파일 행에 동일 배지(reviewing=info·approved=success·rejected=danger) + rejected에서만 빨간 outline 삭제 버튼 노출. 삭제 후에도 rejected 유지, 새 파일 첨부 시 §12 자동 전이 재발동. **§15** 계약 서명 다이얼로그 — 휴대폰 본인인증 sub-step 신규 + 공인인증서 탭 제거. 백엔드 `phone-code` purpose에 'contract_sign' 추가(기존 SHA-256/TTL/5회 제한 인프라 재사용), 다이얼로그 STEP 3에 본인인증 카드(휴대폰 마스킹 + 발송 + 6자리 확인) → 통과 시 success 톤 + 캔버스 셋업. 사용자단 피드백 후속으로 다이얼로그 open watcher에 auth.fetchMe() 강제 hydrate + 공인인증서 탭/CSS·signTab·certLoaded 100여 줄 제거. 정보 테이블 사업자명/대표자 동적 바인딩(`auth.tenant.{name,bizNo,ceoName}`). Workers `85de422a`, Pages `38d4e40e` → `573a6200`. **§16** 운영 노트 — NICE 자격증명 production 등록 시도 → 1007 "허용되지 않은 IP 접근"으로 mock 복귀(secret 3개는 보관). NHN Notification Hub 자격증명 확인 → 신규 통합 서비스가 기존 채널별 NHN과 완전히 다른 인증(`Authorization: Bearer` + OAuth2 client_credentials) → User Access Key 발급 + 어댑터 재작성 필요로 보류. 둘 다 사용자 콘솔 작업 대기. |
| [2026-06-01](./history.20260601.md) | **§1** WBS 정본화 — `doc/WBS.md` 신규(5 단계 가중치·Step 1~5 작업 내역·알려진 한계, ~220 라인) + 사용자단 `/wbs` 공개 라이브 카탈로그(`malgn-helper/doc/WBS.md` 양식 + `malgn-helper-pms/pages/wbs.vue` Notion soft SaaS 디자인 차용 — Hero stats·단계별 진행률 오버뷰·Stage 상세·그룹별 작업 카드·상태 칩·외부 링크 + `layout: blank`·`auth: false`, ~650 라인). Step 5(서비스 개발)는 원본 채널·도메인 단위 항목(대부분 0%)을 실제 진행에 맞춰 5-1 설계(7) / 5-2 API(16) / 5-3 사용자단(15) / 5-4 관리자단(13) / 5-5 통합·배포(7)의 5 그룹 58 작업으로 재정렬. Cloudflare Pages 배포 #47 (alias `0ecc825e.malgn-noti.pages.dev`). **§2** `0002_export_flow.sql` DDL — `wrangler dev --remote` 1105 잔류 3회 재시도 모두 실패(Ray ID `a04a8ca2...`·`a04a9753...`) → Aurora 직결(`noti` 계정·SSL REQUIRED)로 우회 점검. 4 신규 테이블(TB_EXPORT_JOB / TB_FLOW_*) **이미 라이브 적용된 상태 확인** — 컬럼 100% 일치, 인덱스·FK는 라이브가 더 정교(FK 6 + 의미 인덱스명). 라이브 워커 e2e 검증(`/export-jobs`·`/flow-definitions` GET 200 / POST 201) + 테스트 데이터 cleanup + `0002_export_flow.sql` 파일을 라이브 정본에 맞춰 동기화. WBS 5-2-11/12 🟢 (CRUD ✅·처리 worker 미) + 5-5-4 ⛔→✅. **§3** `schema.ts` 정합화 — export/flow 4 테이블에 라이브 정본의 인덱스 6 + FK 6 명시화(`malgn-noti-api: 0475bd2`, schema.ts 단일 파일 +22 -4, 컬럼 정의 변경 0, 다른 테이블 변경 0, typecheck 통과, 런타임 영향 0). drizzle-kit introspect drift 12건 → 0. **§4** 사용자단 인증 백엔드 연동 — `/auth/signup` · `/auth/login` · `/me` 실 API 연동(JWT `auth-token` 쿠키 + `useApi()` Bearer 자동 주입 + 글로벌 미들웨어 1차 가드 + `plugins/auth.client.ts` 클라이언트 부트스트랩 페치). 회원가입 Step 4 → 실 API → 자동 로그인 → `/home`. 로그인은 `last-company-id` 쿠키 자동 사용. 첫 SSR 시도에서 `await auth.fetchMe()`가 Nuxt instance 컨텍스트 손실로 500 → 미들웨어/플러그인 분리로 우회. 6 파일 수정 + 1 신규(`plugins/auth.client.ts`), typecheck 통과, 로컬 + 프로덕션 e2e(쿠키 동봉 시 `/home` 200 · 없으면 `/login?redirect` · `/login`·`/signup`·`/wbs` 200). Pages 배포 #49 (alias `9be4ff61.malgn-noti.pages.dev`). WBS 5-3-15 ⚪ → 🟢. **§5** 이메일 OTP 인증 — `POST /auth/email-code/send`·`/verify` 백엔드 신설(TB_VERIFICATION + SHA-256 코드 해시 + TTL 10분·재발송 시 직전 코드 만료·5회 시도 제한·소비 후 재사용 차단), Drizzle schema.ts에 verification 정의(라이브 정본 정합), OpenAPI 2 paths + 3 schemas, Workers 배포 #9(Version `83f32a61...`). 프런트 signup.vue `sendIdCode`/`confirmIdCode` 실 API 연동(`mockCode` 토스트 노출은 NHN_MOCK=1 한정 — production 자동 차단, 로딩 상태 + 재발송 라벨). Pages 배포 #50(alias `c2100890`). 라이브 e2e 6 시나리오 모두 통과(발송·잘못된 코드 401·올바른 코드 200·소비 후 재시도 401·재발송·DB 행 검증) + 검증 후 임시 NHN_MOCK secret 제거 확인. doc/SIGNUP.md §8 #4 ⚪→✅. |
| [2026-05-27](./history.20260527.md) | **트랙 A** — `malgn-noti-admin` Nuxt 3 + Nuxt UI v3 부트스트랩 + LNB(256px · 8그룹) + TopBar(64px) 셸 레이아웃(`design_handoff_customer_detail` 정본 참조) + 첫 실 Nuxt 앱 프로덕션 배포(`https://malgn-noti-admin.pages.dev`, 이전 정적 placeholder 대체). **트랙 B** — `malgn-noti-api` §19 멱등 버그 정식 해결(TB_IDEMPOTENCY + INSERT-then-conflict race-free) + NHN SMS 어댑터(mock/real) + Cloudflare Queues(`malgn-noti-dispatch`) + consumer worker(`dispatch_state` 천이) + 프로덕션 배포 #6(Version `b30dc2a3...`, Producer+Consumer 동시 바인딩) + NHN webhook 핸들러(`POST /webhooks/nhn/sms`, HMAC-SHA256 서명 검증·dedup_key 멱등·recv_state 자동 천이) + **Email · Kakao(알림톡/친구톡) · Push 3채널 동시 추가**(adapter·producer·worker·OpenAPI 4지점, channel branching generic화·EMAIL_PRICING=0.65·KAKAO_PRICING={alimtalk:8,friendtalk:12}·PUSH_PRICING=0.5) + 프로덕션 배포 #7(Version `12dae362...`). 큐 e2e + 웹훅 검증은 Cloudflare 1105 회복 후 재시도. **트랙 C** — `malgn-noti` 사용자단 추가 작업으로 메시지 관리 랜딩페이지 만들기(목록·기본형/확장형 폼·미리보기) + 문의하기 경로를 `/account/inquiry`로 이동(GNB·푸터·사이트맵 링크 정리) + 나의 페이지 섹션(공통 셸 + 9개 라우트·회원 정보 변경·결제 카드 관리) + 크레딧 충전 플로우(시안 기반 재구성·결제 컨펌·결과 화면) Pages 배포 #44·#45·#46. **트랙 D** — `malgn-noti-api` 루트(`/`) placeholder JSON을 `c.redirect('/doc')`로 교체해 Scalar API 문서로 302 이동, Workers 재배포(Version `f3fd3eb4...`). **트랙 E** — `malgn-noti-api` §13 working tree WIP 정식 커밋 — **RCS 채널(5채널째)** adapter(`rcs.ts` sms/lms/mms/template 4타입)·producer·consumer·webhook + `RCS_PRICING{sms:12,lms:40,mms:120,template:50}` + **Export 잡** `TB_EXPORT_JOB` + `/export-jobs` CRUD(시안 다운로드 요청 PU 대응, 90일 윈도우 우회) + **Flow 정의** `TB_FLOW_DEFINITION/RUN/STEP_RUN` + `/flow-definitions` CRUD(nodes JSON 검증: order 연속·첫 노드 always·5채널 enum) + OpenAPI 4지점(+230) + `0002_export_flow.sql` 마이그레이션 → 단일 배치 커밋 `1e7bd61`(12 files +1228 −16) + 프로덕션 배포 #8(Version `95f9f894...`). DDL 적용은 1105 회복 후 `/admin/migrate` 경로로 보류(프런트 호출처 0개 — 무영향). |

## 작성 시점

- 큰 마일스톤 마무리 직후
- 그날 끝낼 때 짧게라도 한 줄 요약 + 산출물 목록만이라도

## 검색

특정 결정을 언제 했는지 찾을 때 `grep`:

```bash
grep -rn "Smart Placement" doc/history/
grep -rn "Aurora 노출" doc/history/
```
